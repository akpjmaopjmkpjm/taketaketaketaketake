version: 0.0
os: windows
files:
  - source: \
    destination: C:\deploy
file_exists_behavior: OVERWRITE
hooks:
  ApplicationStart:
    # 🔴【修正】locationにはスクリプトの相対パスだけを書きます
    - location: s3_transfer.ps1
      timeout: 300