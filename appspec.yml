version: 0.0
os: windows
files:
  - source: \
    destination: C:\deploy  # 💡（※以前のcurrent_appなど、指定しているフォルダに合わせてください）
    overwrite: true        # 🔴【重要】この行を追加して強制上書きを許可します
hooks:
  ApplicationStart:
    - location: s3_transfer.ps1
      timeout: 300