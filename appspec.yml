version: 0.0
os: windows
files:
  - source: \
    destination: C:\deploy
file_exists_behavior: OVERWRITE
hooks:
  ApplicationStart:
    # 🔴【重要】locationを以下のように書き換えます
    - location: powershell.exe
      args: -ExecutionPolicy Bypass -File C:\deploy\s3_transfer.ps1
      timeout: 300