version: 0.0
os: windows
files:
  - source: \
    destination: C:\hhonbantest
    overwrite: true # 💡 これを追記して、強制上書きモードにします
hooks:
  ApplicationStart:
    - location: s3_transfer.ps1  
      timeout: 300